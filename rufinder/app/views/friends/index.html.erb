<a href=foods>Comidas</a>
<a href=restaurants>Restaurantes</a>
<a href=reviews>Reviews</a>
<a href=friends>Amigos (não implementado ainda)</a>
<h1> Amigos </h1>

<table>

	<tr>
		<th> Amigos </th>
	</tr>

	<% @friends.each do |x| %>
		<% if f.sender_id == current_user.id %>
			<% if f.status %>
				<tr> <%= (User.find_by(id: f.receiver_id)).name %> </tr>
			<% end %> 
		<% end %>
		<% if f.receiver_id == current_user.id %>
			<% if f.status %>
				<tr> <%= (User.find_by(id: f.sender_id)).name %> </tr>
			<% end %> 
		<% end %>
	<% end %>

</table>

<br>

<table>

	<tr>
		<th> Pedidos de Amizade </th>
	</tr>

	<tr> Enviados </tr>

	<% @friends.each do |x| %>
		<% if f.sender_id == current_user.id %>
			<% if !f.status %>
				<tr> <%= (User.find_by(id: f.receiver_id)).name %> </tr>
			<% end %> 
		<% end %>
	<% end %>

	<tr> Recebidos </tr>

	<% @friends.each do |x| %>
		<% if f.reciever_id == current_user.id %>
			<% if !f.status %>
				<tr> <%= (User.find_by(id: f.sender_id)).name %> </tr>
			<% end %> 
		<% end %>
	<% end %>

</table>
	

<a href=main>Voltar</a>

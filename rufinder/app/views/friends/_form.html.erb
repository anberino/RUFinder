<%= form_with scope: :friend, url: :friends, local: true do |f| %>
	<%= f.label :email %>
	<br>
	<%= f.text_field :email,  placeholder:"Email do seu amigo" %>
	<br><br>
	<%= f.submit "Enviar pedido de amizade", class: 'button1'%>
<% end %>
